<div class="flex flex-col items-center">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice">
      <%= notice %>
    </p>
  <% end %>

  <!-- Título e Formulário -->
  <div class="w-full max-w-4xl px-4 mb-7">
    <h1 class="font-bold text-2xl mb-4">Task Manager</h1>
    <div class="flex items-center space-x-2">
      <%= render "form", task: @task %> <!-- Aqui você chama o partial do formulário -->
    </div>
  </div>

  <!-- Tarefas -->
  <div id="tasks" class="w-full max-w-4xl px-4">
    <h1 class="font-bold text-lg mb-7">Tasks</h1>
    <div class="px-5">
      <%= render @tasks %> <!-- Renderiza a lista de tarefas -->
    </div>
  </div>
</div>